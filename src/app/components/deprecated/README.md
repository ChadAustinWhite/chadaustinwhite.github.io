# Deprecated components

These components were superseded by the portfolio redesign (HTML prototype). They are no longer used by the app and can be removed in a future cleanup.

- **Navigation** → replaced by `SiteNav`
- **Hero** → replaced by `HeroSection`
- **FeaturedWork** → replaced by `WorkSection` + `ProjectCard`
- **Clients** → removed from new design (no equivalent section)
- **Contact** → replaced by `Footer` and nav/footer links
- **Case study pages** (`CaseStudy`, `CaseStudyCreativeSpace`, `CaseStudyLuxuryGoods`, `CaseStudyFashionForward`, `CaseStudyTechInnovation`) → replaced by the data-driven case study template in `components/case-study/` (CaseStudyPage, CaseStudyLayout, and reusable section components) with content in `data/caseStudies/`
